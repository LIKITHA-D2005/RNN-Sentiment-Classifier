# RNN-Sentiment-Classifier
RNN-based sentiment analysis model that classifies text as positive or negative. Includes preprocessing steps like tokenization, stemming, and TF-IDF vectorization. Built using LSTM/GRU to capture sequential patterns, demonstrating NLP pipelines and deep learning for text classification.
